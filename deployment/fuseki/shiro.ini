[main]
ssl.enabled = false

plainMatcher=org.apache.shiro.authc.credential.SimpleCredentialsMatcher
iniRealm.credentialsMatcher = $plainMatcher

[users]
admin=${ADMIN_PASSWORD}

[roles]

[urls]
## Control functions open to anyone
/$/status = anon
/$/ping   = anon

## Public query and read-only Graph Store routes
/vietheritage/sparql = anon
/vietheritage/data = anon

## Admin and mutation routes remain protected
/*/update/** = authcBasic,user[admin]
/*/upload/** = authcBasic,user[admin]
/*/data/** = authcBasic,user[admin]
/$/** = authcBasic,user[admin]

## Everything else
/**=anon
